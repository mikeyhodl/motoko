parse-block-or-record.mo:14.14-14.19: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:15.14-15.19: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:21.14-21.19: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:21.21-21.26: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:21.28-21.33: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:26.25-26.30: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:26.32-26.37: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:26.39-26.44: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:31.36-31.41: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:31.43-31.48: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:36.3-36.4: syntax error [M0001], unexpected token ';', expected one of token or <phrase> sequence:
  }
  seplist(<dec>,<semicolon>) }
parse-block-or-record.mo:42.19-42.23: syntax error [M0001], unexpected token 'with', expected one of token or <phrase> sequence:
  }
  .<nat>
  !
  <exp_nullary(ob)>
  ; seplist(<dec>,<semicolon>)
  |> <exp_bin(ob)>
  or <exp_bin(ob)>
  <binop> <exp(ob)>
  <unassign> <exp(ob)>
  <relop> <exp_bin(ob)>
  = <exp(ob)>
  . <id>
  : <typ_nobin>
  := <exp(ob)>
  <binop> <exp_bin(ob)>
  <binassign> <exp(ob)>
  and <exp_bin(ob)>
  <unop> <exp_bin(ob)>
  <inst> <exp_nullary(ob)>
  [ <exp(ob)> ]
parse-block-or-record.mo:42.26-42.27: syntax error [M0001], unexpected token '=', expected one of token or <phrase> sequence:
  }
  .<nat>
  !
  <exp_nullary(ob)>
  ; seplist(<dec>,<semicolon>)
  |> <exp_bin(ob)>
  or <exp_bin(ob)>
  <binop> <exp(ob)>
  <unassign> <exp(ob)>
  <relop> <exp_bin(ob)>
  . <id>
  : <typ_nobin>
  := <exp(ob)>
  <binop> <exp_bin(ob)>
  <binassign> <exp(ob)>
  and <exp_bin(ob)>
  <unop> <exp_bin(ob)>
  <inst> <exp_nullary(ob)>
  [ <exp(ob)> ]
parse-block-or-record.mo:44.16-44.20: syntax error [M0001], unexpected token 'with', expected one of token or <phrase> sequence:
  }
  .<nat>
  !
  <exp_nullary(ob)>
  ; seplist(<dec>,<semicolon>)
  |> <exp_bin(ob)>
  or <exp_bin(ob)>
  <binop> <exp(ob)>
  <unassign> <exp(ob)>
  <relop> <exp_bin(ob)>
  = <exp(ob)>
  . <id>
  : <typ_nobin>
  := <exp(ob)>
  <binop> <exp_bin(ob)>
  <binassign> <exp(ob)>
  and <exp_bin(ob)>
  <unop> <exp_bin(ob)>
  <inst> <exp_nullary(ob)>
  [ <exp(ob)> ]
parse-block-or-record.mo:44.23-44.24: syntax error [M0001], unexpected token '=', expected one of token or <phrase> sequence:
  }
  .<nat>
  !
  <exp_nullary(ob)>
  ; seplist(<dec>,<semicolon>)
  |> <exp_bin(ob)>
  or <exp_bin(ob)>
  <binop> <exp(ob)>
  <unassign> <exp(ob)>
  <relop> <exp_bin(ob)>
  . <id>
  : <typ_nobin>
  := <exp(ob)>
  <binop> <exp_bin(ob)>
  <binassign> <exp(ob)>
  and <exp_bin(ob)>
  <unop> <exp_bin(ob)>
  <inst> <exp_nullary(ob)>
  [ <exp(ob)> ]
parse-block-or-record.mo:50.24-50.29: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:50.31-50.36: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:50.38-50.43: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:51.24-51.29: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:51.31-51.36: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:51.38-51.43: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:53.22-53.27: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:53.29-53.34: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:53.36-53.41: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:57.33-57.38: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:57.40-57.45: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:57.47-57.52: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:61.24-61.29: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:61.31-61.36: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:61.38-61.43: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:62.21-62.26: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:62.28-62.33: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:62.35-62.40: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:64.27-64.32: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:64.34-64.39: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:64.41-64.46: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:66.21-66.26: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:66.28-66.33: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:66.35-66.40: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:70.23-70.28: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:70.30-70.35: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:70.37-70.42: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:71.22-71.27: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:71.29-71.34: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:71.36-71.41: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:77.5-77.10: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:82.5-82.10: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
parse-block-or-record.mo:86.5-86.10: syntax error [M0272], `x = e` is a record field, but this position holds declarations, not a record literal; to declare a variable or field, use `let` (or `var`); to assign, use `:=`; to produce a record from a block, nest it as the block's result: `{ { x = 0 } }`
