syntax-else-chain-legacy.mo:4.38-4.39: syntax error [M0275], unexpected token '2', expected a block `{ ... }`: `switch` and `do` always take a block, and so do the branches or body of `if`/`while`/`for` when the condition or head is written without parentheses (parenthesize it to keep bare branches)
