`old.wasm` has been generated by a pre-removal compiler (release 1.14.1) from `destabilization-crash.mo.txt`:
`moc --legacy-persistence --legacy-actors -c destabilization-crash.mo.txt -o old.wasm`. Classical persistence
can no longer be produced by the current compiler.

That source is kept as `.mo.txt`, not `.mo`, because it calls the `Prim.stableMemory*`
primitives removed in #6378 — as a `.mo` it would be the only file in the tree still
naming them. It is never compiled; `destabilization-crash.drun` installs and upgrades
`old.wasm` itself. Leave it verbatim: the trap location in `ok/destabilization-crash.drun.ok`
is the line number baked into `old.wasm` (41), so editing the source desyncs the golden.
